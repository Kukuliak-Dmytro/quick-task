/**
 * Default search query value when no search is performed.
 */
export const SEARCH_DEFAULT_QUERY = "";

/**
 * URL query parameter name for search functionality.
 */
export const SEARCH_QUERY_PARAM = "search";
